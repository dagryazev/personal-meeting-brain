# Optional: override the embedding model (default: BAAI/bge-m3, 1024-dim, multilingual, runs locally)
# MEETING_BRAIN_EMBED_MODEL=BAAI/bge-m3

# Optional: force device for embeddings. Auto-picks mps on Apple Silicon, cuda on Nvidia, else cpu.
# MEETING_BRAIN_EMBED_DEVICE=mps
